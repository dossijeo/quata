# Inventario de migración multiplataforma

> **Corte vigente (2026-07-29):** `main`
> `d8652326f61d93f33bb860d64565ad74e3e80ed5`. Las referencias a olas anteriores
> que sigan abajo son contexto histórico; el apartado **Corte posterior
> PR #99–#106** prevalece para estado operativo y evidencia.

> El estado operativo, las dependencias, PRs y tareas despachables se mantienen en
> `docs/MULTIPLATFORM_MIGRATION_BOARD.md`. Este documento conserva el detalle arquitectónico.

Reconciliado el 2026-07-27 contra `main` `ea0322c1`: ola 1 (`587789ff`, PR #46)
y ola 2 (`9cc84dc2`, PR #47) están integradas; la rama histórica de ola 2 ya no
existe. Este documento separa
lógica compartible de adaptadores de plataforma; no autoriza mover varias
áreas a la vez ni declara completa la migración.

| Área actual | Estado | Trabajo pendiente |
| --- | --- | --- |
| Web Share Target | Web parcial | El manifiesto Web declara `share_target` multipart para título, texto, URL y archivos. El service worker persiste cada payload y sus Blob en IndexedDB antes de redirigir a `#share-target`; el host Wasm relee el payload persistido, abre el selector común de destinos y elimina/revoca solo al enviar o descartar. Límites actuales: 8 archivos y 25 MB por archivo; un rechazo se muestra en `#share-target-error`. Requiere una PWA instalada y un navegador compatible con Web Share Target. |
| `core` | Parcialmente KMP | Modelos, navegación/deep links, preferencias de sesión/tema/Touch Flow y contratos de cámara, portapapeles, compartir, preferencias, selector de archivos, permisos, ubicación, notificaciones y media se comparten. Android conserva Storage Access Framework, `GetContent`, FileProvider, Media3 y sus launchers. Web usa el selector real (`<input type=file>`) y cámara nativa Web sigue pendiente de host propio. iOS dispone de adaptadores reales para cámara, portapapeles, preferencias, compartir, documentos y ubicación; cámara, compartir y documentos requieren hosts UIKit inyectados y sin ellos devuelven explícitamente `Unsupported`. `IosCoreLocationHost` se inyecta como `LocationService` y `PermissionService`, solicita sólo `when in use`, requiere `NSLocationWhenInUseUsageDescription`, no configura ejecución en background y limita a una solicitud pendiente. `IosCameraPermissionService` consulta y solicita `AVMediaTypeVideo` mediante AVFoundation. `IosKeychainSessionStorage`, `IosImagePickerCameraHost`, `UIDocumentPickerViewController`, `PHPickerViewController`, `UIActivityViewController` y `UNUserNotificationCenter` mantienen los límites ya documentados. Micrófono, Fotos y Archivos continúan explícitamente no disponibles en el contrato iOS. |
| `designsystem` | Parcialmente KMP | Theme, controles base, shell `QuataScreen`, splash, barra inferior y rail de navegación genéricos, pull-to-refresh, rail de acciones de publicaciones, scaffold de editores, Touch Flow, estilos de canvas de texto, ranking, layout de panel flotante, overlay reusable de confirmación, cabecera/fila/banner/input de comentarios, badges de cuenta oficial y presencia de avatar, y renderer de selector emoji comunes; quedan componentes con recursos Android, Coil, cámara, audio y configuración de ventana. |
| Feed | KMP parcial | Dominio, estado, ViewModel, protocolo remoto y agregación de posts/comentarios/likes/perfiles, viewport paginado (pager/recarga mediante slots), chips, scrim, autor/descripción, acción circular, feedback visual de reproducción/rebuffering, barra de controles y scrubber de línea temporal de reel, selector de variante media/fallback, renderer/lector de reels sólo texto, diálogo y proyección de ranking Live (panel, avatar y etiquetas de tipo mediante slots), y la jerarquía estructural de cada reel/tarjeta (media, overlays, rail, overflow y avatar mediante slots) compartidos. El contrato separa `FeedReadRepository` de `FeedMutationRepository`; `ReadOnlyFeedRepository` permite a un host iOS consumir el navegador con un backend de lectura real y devuelve `Unsupported` para mutaciones hasta que éstas estén implementadas. `RemoteFeedReadRepository` comparte mapeo y polling detrás de `FeedReadTransport`; el mapeo escalar a `FeedRemote*` es común, mientras iOS conserva URLSession y Web conserva fetch/PostgREST, sus columnas y errores HTTP/RLS. Los errores de DTO incompleto permanecen propios del adaptador. iOS inyecta transporte autenticado y configuración pública, sin credenciales en commonMain. Compartir texto usa `ShareService`. Web dispone de PostgREST autenticado de sólo lectura/polling y detalle directo para `#post-<id>` mediante `refreshPost`; sus mutaciones, media y Realtime no se declaran disponibles. Media3, Coil, recursos, navegación y cliente/DTO Android siguen como adaptadores. MP-A10 eliminó cuatro wrappers Android sin uso (`ReelScrims`, `ReelTopChips`, `ReelRoundChip`, `ReelChip`) que sólo reexpresaban contenido ya común; no retira adaptadores de plataforma. |
| Chat | KMP parcial; cierres focales integrados | Dominio, estado, ViewModels, envelope RPC y UI estructural son comunes; compila para Wasm. `WebChatRepository` ofrece lectura autenticada/polling y mutaciones aprobadas, incluido adjunto Storage. #226 integró composer/envío, reply/edit, acciones seleccionadas y mute/unmute; #228 reenvío; #231 traducción dentro de Chat; #261–#263 picker, negativos deterministas y rollback upload→registro; #311 sustituyó la evidencia optimista de audio por estados procedentes de eventos nativos y cerró el visor documental focal. Los runners usan anclas o bridges localhost-only acotados cuando el canvas Compose no expone DOM/AX estable, con verificación backend y limpieza reversible. No hay GO global de `SCR-CHAT`: Realtime completo, typing/contactos, permisos OS reales exhaustivos y otros errores/rollback continúan abiertos. |
| Official | KMP parcial | Dominio, estado, eventos, ViewModel, protocolo remoto y mapeo de posts/comentarios/likes/perfiles, ranking, transformaciones HTML y bloques Compose de carga/vacío/paginación, autor con slot de avatar, tipología, texto y lectura compartidos. El viewport del feed, la jerarquía de página del pager y tarjeta responsive, rail de acciones con etiquetas inyectadas, el marco de preview, el flujo completo de preview portrait del editor con panel read-more, el shell del panel de detalle, los shells de pantalla scrollable, secciones, editor largo y preview de media, los controles avanzados de título/resumen/tipo/read-more/enlace, el botón de publicación, el flujo estructural completo del formulario del editor mediante slots y los diálogos de borrado/traducción con su loader animado viven en `commonMain`; avatar, media, renderer HTML y overflow se inyectan por plataforma. Web ofrece lectura PostgREST/polling y host de deep link Official; data source Android, mutaciones, Coil, visor de media y navegación continúan en adaptación progresiva; compartir texto usa el contrato `ShareService`. |
| Post Composer | Raíz común integrada (#154); capacidades de publicación limitadas | La antigua fila de fallback/shell iOS quedó obsoleta: #154 integró `CreatePostRoot` en `commonMain` y sus montajes Android/Web/iOS, con CI exacta acreditada. El host de simulador estrecho fue comprobado, pero no acredita publicación ni paridad visual. Se conservan sin elevar las capacidades: `composer.publish` Web es **contract-only** y iOS **blocked** en `capabilities/platform-capability-matrix.json` hasta E2E real. Cámara de vídeo, previews/edición bitmap/vídeo, exportación y mutaciones siguen pendientes; no se inventan datos Swift. |
| Profile / SOS | Raíz común integrada; `ACCOUNT-DETAILS` #319 y `FLOW-SOS-DISPATCH` #344 con GO focal | `ProfileScreenHost` se monta en Android, Web e iOS; UI, estado y contratos viven en `commonMain`, mientras Android conserva recursos/adaptadores e iOS Keychain, PostgREST y ContactsUI. Web usa persistencia **Remote** sólo con configuración pública y sesión autenticada; en cualquier otro caso queda **Unavailable**, sin fallback local presentado como sincronizado. #319 integró edición, persistencia, recarga y restauración reversible de Mis datos en las tres plataformas. #344 integró el coordinador y adaptadores SOS; la aceptación real acreditó destinatario/mensaje exactos, unicidad, camino sin ubicación, cooldown actor-bound, rechazo de spoof y limpieza. Los GO son focales: no cierran recovery/password legacy/avatar/ciclo de cuenta, importación/normalización de agenda, permisos/sensores exhaustivos, background ni `SCR-SOS` completo. |
| Auth | KMP parcial | Contrato sin `Context`, casos de uso, estados, eventos, ViewModels, shell responsive, shell de sesión autenticada y formularios Compose de login/registro/recuperación viven en `:feature:auth/commonMain`, ahora compilables también para Wasm. Web implementa `AuthRepository` real para `web_login`/logout y recuperación (`recovery_question`/`reset_password`) mediante el bridge público configurado, persiste access/refresh/web-session token y refresca con el endpoint público de Supabase; `PasswordRecoveryQuestion.userId` es opcional porque el bridge Web sólo publica la pregunta. PR #106 integró la superficie de logout autenticado del host iOS. El [postflight iOS documentado en `c485fd45`](https://github.com/dossijeo/quata/commit/c485fd452ca086830f3ac9d50812d1c05ff7db40) ejecutó login real mediante `.xctestrun` y `QUATA_IOS_AUTH_E2E_FILE`, comprobó restauración de sesión tras relanzar sin reinstalar y validó visualmente Cuenta/Perfil sobre `main` `5d2a52d1`. Ese resultado no acredita refresh ni sesión expirada, logout visual/remoto completo, todas las rutas de retorno, registro o Google Sign-In; esos alcances siguen pendientes. |
| Communities (`neighborhoods`) | KMP parcial; host iOS integrado | Dominio, estado, ViewModel, listado, tarjetas, miembros, seguidores/seguidos, filas de usuario, cabecera de perfil mediante slots, KPI, lista y tarjetas de adjuntos con thumbnail inyectable, shell modal y scrollable de detalles de perfil con cabecera/adjuntos/galería inyectables, pager de posts con estado local de comentarios y slots de preview/diálogo, overlay de comentarios de galería con estado de borrador común, panel y tarjeta estructural de comentarios, frame de preview y rail de acciones de post de perfil, y acciones de perfil/roles Compose compartidos en `:feature:neighborhoods`. `3d496f8` compone un host UIKit autenticado con snapshots PostgREST/URLSession y reutiliza el repositorio Chat real para conversaciones; CI iOS exacta #30170903885 pasó (Kotlin/Native, enlace, Swift y XCTest). No declara deep link público, mutaciones Communities, E2E, media ni roles iOS. Caché, data source, reproducción de vídeo y adaptadores Android permanecen pendientes. |
| Notifications | KMP parcial; Reply multiplataforma con GO focal | Dominio, estado, ViewModel, host de presentación y pantalla Compose (lista, tarjetas y descarte) compartidos en `:feature:notifications`; el mapeo de conversaciones no leídas a notificaciones también es común. Android aporta recursos, reloj relativo y callbacks de navegación; Web dispone de `WebNotificationsRepository` real sobre el inbox autenticado de Chat. Web Push acredita dispatch real y delivery log `sent`; la corrección `ad018b5a` acredita clic nativo, `notificationclick` y ruta Chat exacta. El segmento autenticado posterior acredita el marcador escrito en esa conversación, un solo Send, actor/conversación exactos, persistencia única y limpieza recuperada sin residuos. La composición cierra `FLOW-NOTIFICATION-REPLY` en Web sin atribuir texto inline a la API Web. El payload de lanzamiento y resolución de deep links de chat son comunes e independientes de proveedor. Canales, ciclo de token/suscripción, push, receivers y data source siguen siendo adaptadores Android/Web/iOS. |
| About / release history (`whatsnew`) | GO focal integrado en #308 | `QuataAboutDialogContent`, `ReleaseHistoryContent`, el catálogo legal y el chrome común del visor se ejercitaron en Android, Web/Wasm e iOS sobre Product/Evidence SHA `40d4b330`; [PR #308](https://github.com/dossijeo/quata/pull/308) integró el head exacto con certificación final verde. Caché, WorkManager, recursos y window insets siguen como adaptadores Android. El GO no se extiende al visor documental global: adjuntos no legales, descarga/compartir y fallos/rollback permanecen abiertos. |
| External share | GO focal integrado en #314 | `ExternalShareDestinationHostContent` concentra en `commonMain` preview, búsqueda/selección de destino, envío, progreso/error, bloqueo de cierre y descarte; Android, Web/Wasm e iOS conservan únicamente los bordes de recepción, inbox/Storage y apertura de adjuntos. Product/Evidence SHA `b63d1dde` acredita Web con IndexedDB real, envío autenticado de texto+URL+Blob, verificación backend y limpieza física de Storage; Android con parsing/límites de intents y disponibilidad; e iOS con compilación, contratos, Share Extension y 10/10 tests de extensión. [PR #314](https://github.com/dossijeo/quata/pull/314) fusionó el flujo con gates finales Web/Android, iOS y CodeQL verdes. No acredita matrices no deterministas de share sheets/apps externas, una PWA instalada por el SO ni App Group firmado/dispositivo físico. |
| Traductor Fang | KMP parcial | Modelos, protocolo HTTP, errores, calentamiento, caso de uso y estado de cada bloque viven en `core/commonMain`; el registro, modificador de texto, trigger y renderer de fondo Compose viven en `designsystem/commonMain`. FastText, OkHttp, caché SQLite, captura de fondo y textura/recurso localizado siguen como adaptadores Android. |
| Quata Touch Flow | KMP | El efecto, su estado, geometría, animación y renderer Compose viven en `designsystem/commonMain`; su contrato de preferencia por usuario vive en `core/commonMain` y `SharedPreferences` es el adaptador Android actual. |
| Fondos procedurales de chat | KMP parcial | La semilla FNV-1a, selección de paleta, clave de caché y renderer Compose portable viven en `designsystem/commonMain`. `ProceduralChatBackground` conserva EGL/GLES, `Bitmap`, ficheros WebP y `Context` como optimización Android; faltan adaptadores JS/iOS y un contrato de almacenamiento binario para persistir imágenes. |
| Settings | GO focal integrado en #295 | `SettingsScreenHost` concentra en `commonMain` apariencia, documentos legales, estado/acción de notificaciones, ciclo de cuenta y logout; Android, Web/Wasm e iOS inyectan los bordes nativos. [PR #295](https://github.com/dossijeo/quata/pull/295) acreditó Web/Cuenta/Ajustes, Android instrumented e iOS XCTest sobre Product/Evidence SHA `8bfae37c` y se integró con certificación final verde. Las preferencias y almacenamientos propios de plataforma permanecen como adaptadores; push real, borrado/desactivación de cuenta, rollback forzado de logout y el visor no legal siguen fuera del GO focal. |
| Visor de documentos | KMP parcial | La detección y descriptor común de formato (`PDF`, Office, RTF, texto o no soportado) viven en `core/commonMain`, y el frame Compose de preview con estado `Loading/Ready/Unavailable` y slots de renderer/placeholder vive en `designsystem/commonMain`. `core/androidMain` normaliza MIME y referencias antes de inyectar el lector Android existente: sólo entrega `content://`/HTTPS, convierte ficheros internos a `FileProvider` y rechaza URI inválida, de modo que no cruza un `file://` entre Activities. Web carga perezosamente `@docmentis/udoc-viewer` 0.7.9 desde `wasmJsMain` para PDF/DOCX/PPTX/XLSX y destruye cliente/visor al cerrar; sólo acepta HTTP(S) sin credenciales y conserva la descarga segura para RTF, formatos legacy, URL inválida y errores. El paquete JS se declara MIT, pero su runtime Wasm tiene licencia DocMentis propia: sin licencia, la versión 0.7.9 requiere permiso online por apertura, conserva telemetría, comprobación de actualización y posible descarga de Google Fonts por defecto. Quata no configura licencia ni desactiva esos comportamientos; producto/legal y CSP deben aprobar sus orígenes antes de desplegar. Existe automatización para bundle, tests Wasm y smoke de importación/create/destroy, pero sigue pendiente evidencia funcional con documentos propios, CORS y Storage autenticado. DocMentis es Web-only; `document-reader`, lectura de archivo, miniaturas/bitmap y renderer integrado siguen como adaptadores Android. |
| Texto enriquecido | KMP parcial; body de Official con GO focal en #313 | Motor, estado, parser, acciones, runtime, renderer Compose y diálogos portables de nivel de título/enlace viven en `designsystem/commonMain`. #313 integró entrada y persistencia del body desde el editor común de Official en Web/Wasm, Android e iOS, sin bridge HTML directo ni prefill iOS. El editor visual Android conserva recursos, contexto y portapapeles; toolbar, formato y enlaces globales fuera de Official no quedan cerrados. |
| Foto y vídeo | KMP parcial | Perfiles de vídeo, metadatos, rotación, geometría/presets de recorte, contratos multimedia, muestreo, banner de política de duración y transformaciones de orientación de imagen viven en `commonMain`; editores, Media3, cámara, `ContentResolver` y almacenamiento requieren adaptadores `FilePicker`, `Camera`, `VideoEditor` y `MediaStore`. iOS dispone de thumbnail AVFoundation sólo para referencias locales y decodificables; la admisión se prueba desde XCTest sin declarar éxito de decodificación de un fixture. |
| Subtítulos y transcripción de vídeo | KMP parcial | Segmentos, tiempos, estilos, especificaciones de plantilla, animación y layout de palabras viven en `core/commonMain`; `CaptionBitmapRenderer` adapta tipografías/métricas/dibujo Android, y Media3 burn-in, Vosk y entrega de modelo permanecen como adaptadores. |
| Editor de imagen | KMP parcial | Modo de edición y perfiles de salida de post/avatar viven en `feature:postcomposer/commonMain`; estado, operaciones de edición, renderer Compose, bitmap y almacenamiento siguen Android. |
| Audio (grabación, reproducción y adjuntos) | KMP parcial; garantía focal Chat integrada en #311 | El trigger y la superficie visual viven en `designsystem/commonMain`/`feature:chat/commonMain`; `AudioRecorderService`/`AudioPlayerService` y sus estados viven en `core/commonMain`. Android usa Media3/AAC, Web `HTMLAudioElement` y `MediaRecorder`, e iOS hosts AVFoundation inyectados. #311 integró evidencia Web/Android/iOS que exige eventos nativos para `Playing/Ended/Failed`, seek y reproducción consecutiva, además del compositor común y limpieza reversible; invalida como garantía cualquier estado optimista anterior. Persisten permisos/grabación física exhaustivos, routing y lifecycle completo tras subida, errores de red y otras superficies de audio fuera de Chat. |
| Presencia, proximidad y ranking | KMP parcial | Contratos/repositorio de presencia pueden compartirse; proximidad, conectividad, Supabase y UI de ranking requieren separar fuentes de sistema y renderer Compose. |
| Moderación, términos y documentos legales | KMP parcial; documentos legales con GO focal | Modelos, catálogo, rutas, assets locales, enlaces y estado/chrome del visor legal son comunes; #241 y #295 integraron el flujo focal de documentos legales en Android, Web/Wasm e iOS. La aceptación persistente de términos, los adaptadores nativos de apertura (`FileProvider` incluido) y el visor de adjuntos no legales conservan sus alcances separados. |
| Navegación y shell de aplicación | KMP parcial | Destinos y deep links comunes, y el contenido Compose de los diálogos About y autenticación requerida; `AppNavGraph`, barras de sistema, splash, `MainActivity`, enlaces legales y control de ventana deben reducirse a launcher/adaptadores de plataforma. |
| Host Web | Parcial | Kotlin/Wasm es el único producto Web. La distribución y el smoke ola 2 pasaron para Auth, Feed, Chat, Official, Settings y Share Target. Login/logout y sesión usan configuración pública; el alta permanece `fail-closed` hasta existir un endpoint seguro. Profile remoto es condicional a sesión/configuración. Feed/Official conservan lectura/polling; mutaciones no acreditadas siguen deshabilitadas. Chat tiene transporte real, pero su E2E UI está bloqueado por accesibilidad DOM/AX del canvas Compose y no acredita envío/reply/logout. Web Push acredita dispatch del proveedor y delivery log `sent`; la corrección `ad018b5a` verifica activación nativa, `notificationclick` y ruta Chat sintética exacta. El Reply autenticado completo no está verificado. |
| Host iOS | Parcial, host real | El composition root UIKit/Swift, Keychain, hosts Compose y adaptadores están cableados. #30210875187 pasó sobre `9cc84dc2` Kotlin/Native, enlace/XCFramework, host Swift + Share Extension, simulador/XCTest y archive sin firma. La VM local validada usa Xcode 26.6; su [identidad Personal Team y preflight integrados en #332](IOS_LOCAL_DEVELOPMENT_SIGNING.md) no acreditan aprovisionamiento ni distribución. APNs sólo acredita plumbing, nunca entrega; External Share no acredita App Group firmado ni dispositivo físico. El host iOS no se declara listo. |
| Componentes de comunidades | KMP parcial | El renderer de emoji, fila/banner/input de comentarios, ranking y layout de panel flotante ya viven en `designsystem/commonMain`; catálogo localizado, cierre por toque exterior, traducción visual y contenedor Android siguen como adaptadores. |

## Actualizaciones recientes

### Corte posterior PR #99–#106

- **Web público:** PR #99 (`f38503b9`) incorporó cinco muestras frías de
  rendimiento como observación; PR #100 (`be56cacf`) cubrió deep links,
  recarga, tres viewports, foco y árbol de accesibilidad. No convierte el
  presupuesto técnico en SLO ni el recorrido público en E2E autenticado.
- **Capacidades y rutas:** PR #101 (`54a2f07e`) añadió el manifiesto de
  capacidades fail-closed; PR #103 (`9344b5fa`) fija contratos de rutas y
  factorías iOS. Ambos gates delimitan soporte, no reemplazan backend, permisos
  o interacción real.
- **iOS público:** PR #102 (`32f1bb65`) integró el arnés de matriz pública para
  los dos simuladores. El run `30425431607`, job `90490809295`, fue verde sobre
  el SHA de la PR. Feed público, HTTP 200, ausencia observada de crash/fatal,
  OCR/capturas y cleanup son evidencia funcional CPU-raster, no rendimiento ni
  distribución.
- **Distribución y APNs:** el archive genérico sin firma está entregado como
  evidencia de estructura. La [identidad Apple Development local y su preflight](IOS_LOCAL_DEVELOPMENT_SIGNING.md)
  están integrados en #332; el Personal Team aún necesita un dispositivo registrado
  para generar perfiles y no permite distribución. No hay IPA ni TestFlight.
  PR #104 (`18596076`) documenta los requisitos APNs y el
  hallazgo de que el dispatcher necesita un canal APNs separado; no hay entrega
  APNs acreditada.
- **Seguridad:** PRs #99–#106 no cambian Supabase, RLS, DDL, funciones, grants
  ni datos. Las políticas existentes y el Feed anónimo continúan intactos.
- **Logout y autenticación iOS:** PR #106 (`d8652326`) integró una UI de logout
  autenticada y el carril iOS `30429034347` terminó verde. Una comprobación
  redactada de backend obtuvo sesión y perfil de la cuenta de prueba, sin
  persistir secretos. Esto no acredita login, refresh ni logout visual: faltan
  un fichero de configuración remoto `0600` y aislamiento Keychain/test host.
  PR #107 se cerró sin merge tras una desconexión del proceso Compose causada
  por su fixture.

- **Localización de capacidades (Vertical integrada):** los avisos de disponibilidad usan ahora un catálogo mínimo en `core/commonMain` para español e inglés, resuelto por etiqueta de idioma y con fallback español probado. El lote `c8cf7df` pasó `:core:wasmJsTest`, compilación Web/Android, bundle+smoke Web y CI iOS `#30168079601` (Kotlin/Native, framework, host Swift y XCTest). Esta primera vertical no migra ni reescribe los demás textos de Web, Android o iOS.
- **Matriz de capacidades:** `FeatureCapabilityManifest` v1 vive en `core/commonMain` y distingue compilación, exportación, composición, navegación, backend real y E2E del origen de datos/acciones (`Real`, `Local`, `Unsupported`). Los launchers inyectan su registry. Web consume la proyección para etiquetar el estado sin ocultar rutas ya cableadas; Profile declara persistencia local y las mutaciones Feed/Communities/Official siguen deshabilitadas por contrato, no por una falsa confirmación de guardado.
- **Evidencia de hosts:** ola 1 está en `main` por PR #46 (`587789ff`). Para ola 2 (`9cc84dc2`) pasaron localmente compilaciones Wasm, distribución y smoke Chrome con DocMentis, además de assemble/A-B Android; la CI iOS #30210875187 terminó verde. Ver `mp-a14-final-evidence.json`. Son gates distintos y no una declaración de Web/iOS completos.
- **E2E remoto:** SB-01 a SB-06 tienen evidencia y purga. SB-07 halló RLS-001 y SB-09 halló RLS-002; no se cambiaron políticas y las mutaciones afectadas siguen `fail-closed`. SB-08 continúa bloqueado por credenciales/dispositivo. El Chat browser autenticó dos cuentas y creó el hilo, pero la UI quedó bloqueada por DOM/AX; se verificaron cero residuos.

- **Validación Android API-37:** el ANR previo se reprodujo también en el baseline
  bajo presión de CPU y no se atribuyó al corte Feed. Tras reiniciar el emulador,
  el SHA de integración MP-A10 arrancó en frío en 3,04 s, mantuvo PID y no produjo
  entradas en el buffer `crash`; la reducción no alteró los adaptadores Android.

- Post Composer: `ComposerMediaPostPreviewContent` concentra en `feature:postcomposer/commonMain` la jerarquía de preview de imagen/vídeo (frame, scrim, chips, rail y autor), dejando al host únicamente el slot de media nativa/navegador, textos localizados y avatar opcional.
- iOS dispone de `IosAudioCacheService` para copiar, recuperar y eliminar ficheros de audio locales en el temporal de la app. El contrato `AudioCacheService` rechaza claves inseguras y referencias no locales; no descarga media ni sustituye el host AVFoundation.

## Orden de ejecución

1. Auth: crear módulo KMP y extraer contrato, estados, eventos y ViewModels; Google Sign-In queda como adaptador.
2. Communities y Notifications: un módulo por feature, sin mover sus repositorios Android.
3. About/release history y External share: extraer primero modelos y lógica sin intents.
4. SOS: separar coordinador común de ubicación, permisos, notificaciones e intents Android.
5. Traductor Fang: migrar el renderer Compose del overlay; conservar detector, caché y captura como adaptadores de plataforma.
6. Quata Touch Flow: extraer la preferencia por usuario mediante el contrato multiplataforma de preferencias.
7. Fondos procedurales de chat: extraer semilla, selección de paleta, clave y contrato de renderizado; mantener EGL/GLES, WebP y ficheros como adaptadores hasta contar con implementaciones JS/iOS.
8. Rich text, documento y multimedia: definir contratos de plataforma y mover sólo la lógica/UI que compile en `commonMain`.
